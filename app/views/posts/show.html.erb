<div>
  <%= attachment_image_tag @post, :image, width: '20%', height: 'auto', fallback: "noimage.jpg" %>
   <p>投稿日時：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
  <p>song title：<%= @post.title %></p>
  <p>artist：<%= @post.artist %></p>
  <p>event：<%= @post.event %></p>
  <p>caption：<%= @post.body %></p>
  <p>user name：<%= @post.user.name %></p>
</div>