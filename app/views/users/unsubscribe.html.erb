<div id="user-header" class="big-bg">
  <%= render 'layouts/header' %>
</div>

<div class='container'>
  <div class='row justify-content-center'>
    <h3>本当に退会しますか？</h3>
  </div>
  <div class="row justify-content-center">
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div class="row justify-content-center mb-5">
    <section class="user-del1">
      <%= link_to '退会しない', user_path(current_user.id), class: 'btn_7 no-underline' %>
    </section>
    <div class="user-del2">
      <%= link_to "退会する", withdraw_path, method: :patch, "data-confirm" => "本当に退会しますか?", class: 'btn_6 no-underline' %>
    </div>
  </div>
</div>